<div class="container">
  <header>
    <div class="header-container">

      <h1>Dead <span class="text-image">Runners</span> Prod.</h1>
      <h2>Royalty free beats by Dead Runners Productions.</h2>
      <h4>No licenses. All 100% royalty free.</h4>
    </div>
  </header>

</div>
<div class="container">
  <!-- <div class="btn-container text-center">
    <span><strong>Filter:</strong> </span>
    <button class="btn" type="button" name="button" onclick="filterSelection('all')">All</button>
    <button class="btn" type="button" name="button" onclick="filterSelection('chill')">Chill</button>
    <button class="btn" type="button" name="button" onclick="filterSelection('distorted')">Distorted</button>
    <button class="btn" type="button" name="button" onclick="filterSelection('lo-fi')">Lo-fi</button>
    <button class="btn" type="button" name="button" onclick="filterSelection('old-school')">Old School</button>
    <button class="btn" type="button" name="button" onclick="filterSelection('trap')">Trap</button>
  </div> -->
  <div class="row">
<% for(var i=0; i< tracks.length; i++) {%>
    <div class="song-tile col-sm-12 col-md-6 col-lg-4 text-center <%= tracks[i].tags.slice(',').join(' ') %> show">
      <div class="song_image">
        <img class="track-image" src="<%= tracks[i].img %>" alt="">
        <audio id="player<%=tracks[i]._id %>" src="<%= tracks[i].demo_source %>" loop></audio>
        <div class="audio-player">
          <button class="play" onclick="document.getElementById('player<%=tracks[i]._id %>').play()"><i class="fas fa-play fa-4x"></i></button>
          <button class="pause hide" onclick="document.getElementById('player<%=tracks[i]._id %>').pause()"><i class="fas fa-pause fa-4x"></i></button>
        </div>
      </div>
      <h2><%= tracks[i].name %></h2>
      <p><strong>Tags:</strong>
        <% for(let j = 0; j < tracks[i].tags.length; j++){ %>
          <span><%=tracks[i].tags[j]%></span>
        <% } %>
      </p>
      <p>
        <%= tracks[i].description %>
      </p>
      <h5>£<%= tracks[i].price / 100 %></h5>
      <form action="/charge" method="POST">
        <input type="hidden" name="name" value="<%= tracks[i].name %>">
        <input type="hidden" name="description" value="<%= tracks[i].description %>">
        <input type="hidden" name="total_source" value="<%= tracks[i].total_source %>">
        <script
          src="https://checkout.stripe.com/checkout.js" class="stripe-button"
          data-key="pk_test_CGQfgCOAuB880dmwwD4LWaiS"
          data-amount= <%= tracks[i].price %>
          data-name="<%= tracks[i].name %>"
          data-locale="auto"
          data-label="Buy Now"
          data-currency="gbp">
        </script>
      </form>
    </div>
  <% } %>
  </div>
</div>
<div class="container text-center">
  <small>* All downloads are single mp3 files. If you like a song and wish to purchase the excluse rights, please get in touch at <strong>hello@blackoutprod.com</strong>.</small>
</div>
